# Required
DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/noir?sslmode=require
SESSION_SECRET=replace-with-a-long-random-string

# Optional — owner seed credentials
ADMIN_EMAIL=owner@noirclub.com
ADMIN_PASSWORD=noir2026

# Optional — SMTP for email alerts
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@example.com

# Optional — CORS
ALLOWED_ORIGINS=

# Optional — Memory rate limiter fallback
# RATE_LIMIT_STORE=memory
# REDIS_URL=redis://...
